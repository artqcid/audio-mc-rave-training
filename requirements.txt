# Core GUI and audio dependencies for the RAVE/RVC training app.
# Install PyTorch separately to match the correct CUDA version for your WSL setup.
fastapi==0.114.0
uvicorn[standard]==0.23.2
jinja2==3.1.2
aiofiles==23.1.0
python-multipart==0.0.6
pydantic==2.9.0
numpy==1.26.4
scipy==1.11.4
soundfile==0.14.0
librosa==0.11.0
pandas==2.2.3
python-dotenv==1.0.0
# Optional audio ML helpers
torchaudio==2.2.1
# NOTE: Install torch separately with the correct CUDA variant for your environment.
# Example: pip install torch --index-url https://download.pytorch.org/whl/cu124
